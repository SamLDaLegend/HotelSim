{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM"],
    "types": []
  },
  "//": "vite.config.ts is included because `check-ladder.mjs` scans the whole of apps/game, so a file the gate judges must be a file the compiler judges too (G-030).",
  "include": ["src/**/*.ts", "vite.config.ts"]
}
